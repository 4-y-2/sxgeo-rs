# sxgeo-rs

Fast, unofficial pure Rust parser for SypexGeo databases.

## Features
- Fast parsing of `.dat` files
- No external heavy dependencies
- Pure Rust implementation

## Installation

Add this to your `Cargo.toml`:

```toml
[dependencies]
sxgeo-rs = "0.1.1"
